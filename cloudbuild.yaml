steps:
  - name: gcr.io/cloud-builders/docker
    args: ['build', '-f', 'backend/Dockerfile', '-t', 'gcr.io/$PROJECT_ID/granted-robotics-api', '.']
images:
  - gcr.io/$PROJECT_ID/granted-robotics-api
options:
  logging: CLOUD_LOGGING_ONLY
